# 成本記錄與估算（Google API / LLM 花費追蹤）

> 用途：定期把各外部 API 的用量與花費記在這，及早發現爆量、對照護欄是否夠。
> ⚠️ 下方單價為**概估、會變動**，一律**以 Google Cloud 帳單與各家定價頁為準**；Google 通常另有每月免費額度（以帳單頁為準）。
> 相關護欄程式：`supabase/functions/ai-proxy/index.ts`（限額/快取）；規模化升級見 `產品筆記與待辦.md` §11。

---

## 1. 成本項目與概估單價（參考，需以帳單校正）

| 項目 | API / action | 概估單價（USD，需校正） | 走哪 | 現有護欄 |
|---|---|---|---|---|
| 地點搜尋 | Places Text Search (New)`place-search` | 約 $32 / 1000 次（依欄位 SKU 不同） | ai-proxy | 每日限額＋cached_searches(7天)＋debounce/2字/第一頁快取＋分頁封頂 |
| 找點/解析 | Places Text Search `findplace`/`resolve-place` | 同上 | ai-proxy | 每日限額＋cached_locations |
| 地理編碼 | Geocoding `geocode` | 約 $5 / 1000 次 | ai-proxy | 每日限額＋cached_locations |
| 路線 | Directions `directions` | 約 $5 / 1000 次（基本） | ai-proxy | （精算尚未大量使用；規劃用估算） |
| 地點細節/評分 | Places Place Details（帶 `rating`/`userRatingCount`）| 依欄位 SKU；`rating`＝Enterprise SKU，較貴 | ai-proxy | **【D2 評分 方案A】只在「存進清單/開地點細節」時查一次，不在搜尋列表每筆帶**＋每日 guard＋快取 |
| 地點補全＋評分 | Places Text Search top1（帶 rating）`place-lookup` | 同上 Enterprise SKU | ai-proxy | **【D2②-A】enrich：先試 details，仍無評分才 lookup 一次拿商家 POI 的 placeId＋rating**。搜尋加的＝1 次(details)；匯入(Geocoding 給地址級 id)＝最壞 2 次(details 沒中＋lookup)＋每日 guard；命中寫入 details 快取 |
| 貼連結解析 | `resolve-maps-url` / `coordsFromMapsUrl` | 多數免費（純解析 URL 抽座標）；抽不到才 fallback 一次 resolve | ai-proxy | 每日限額＋cached_locations（重用既有） |
| 生成/文字 | Gemini `gemini-text` / `gemini-vision` | 依 token（model 而定） | ai-proxy | 付費牆（4b opt-in）＋生成快取(cacheKey) |
| 天氣/時區 | `weather` / `timezone` | 依供應商 | ai-proxy | — |

---

## 2. 目前的成本護欄（已在，2026-07）

- **每人每日 200 次 Google 呼叫硬限額**（搜尋＋geocode 共用；`geocode_usage` 表）。滿了回空、不再打 Google → **單一使用者一天花費有硬天花板**。
- **全域快取**：`cached_locations`（geocode/findplace）、`cached_searches`（搜尋，TTL 7 天）。快取命中＝免費、不計數。
- **前端**：debounce 0.35 秒、至少 2 字才查、第一頁快取。
- **分頁封頂**：Places 一個關鍵字約 3 頁 / 60 筆，token 撈完就停 → 「更多結果」不可能無限點。

---

## 3. 待加護欄（成本升高時再上；對應 §11 規模化清單）

- [ ] **搜尋獨立每日額度**（與 geocode 分流，單獨鎖搜尋花費、不餓死地理編碼）。
- [ ] **分頁結果也快取**（現在只快取第一頁；同關鍵字重複「更多結果」→ 免費）。
- [ ] **預算 guard／爆量告警**（花費接近門檻通知）。
- [ ] **搜尋結果照片/營業時間欄位** 會提高 Places SKU → 加時記成本。
- [ ] **免費層每日上限 ＋ 付費層放寬**（商業模式對齊：重度搜尋＝高意圖＝付費甜蜜點）。
- [ ] **「更多結果」點按次數上限**（實測可按 >5 次＝分頁比預期鬆，每次都打一次 API）。目前仍受每日 200 硬限額擋著，但可加「單次搜尋最多載入 N 頁」再省。Kelvin：D2.3 後決定。
- [ ] **評分/營業時間欄位＝Places SKU 升級（重要成本點）**：加 `rating` → Enterprise SKU；加 `openingHours` → Enterprise+Atmosphere SKU（**最貴**）。每筆搜尋結果都帶＝成本明顯上升。
  - **【D2 評分 定案・方案A】** 評分**只在「存進清單／開地點細節」時查一次** Place Details（帶 `rating`/`userRatingCount`），**不在搜尋列表每筆帶** → 由 O(結果數) 降為 O(1)。顯示位置：**地點細節卡 ＋ 清單項目**。要加：(1) 評分查詢納入每日 200 硬限額（與搜尋/geocode 共用計數）；(2) rating 結果快取（同 placeId 不重複打）；(3) 用量進下方 §4 記錄。營業把關同樣「選定時查 Details 一次」，不每筆帶 openingHours（避 Atmosphere 最貴 SKU）。
- [ ] **貼連結（D2）**：優先純解析 URL 抽座標（免費），抽不到才 fallback 一次 resolve；重用 `cached_locations`。低成本，但仍受每日 200 硬限額保護。

---

## 3.5 天氣（批4臉1 · 旅途中 hero 天氣膠囊）
- **供應商：Open-Meteo（open-meteo.com）**。費用：**$0**（非商用免費、無需 API key、無需信用卡）→ **無金鑰外洩面、無 proxy 需求**，前端直呼。
- 護欄：模組級快取 1 小時（座標粗化到小數 2 位）；**失敗也快取**（斷網不重打）；拿不到＝UI 靜默消失（不佔位）。
- 用量級別：每位旅途中使用者每小時最多 1 次呼叫，遠低於其免費上限（10k/日）。上架若轉商用需再評估其商用授權或換源。

## 3.6 封面B · 目的地嚮往照（Pexels）
- **供應商：Pexels API**。費用：**$0**（免費 20k 次/月、200 次/小時；無強制署名）。金鑰 `PEXELS_KEY` **只放 ai-proxy server env**（新 action `cover-photo`），客戶端拿不到。
- 護欄：**一趟一張**——抓到 URL 即存進 trip_data，之後零呼叫（消耗＝新建行程數，月耗個位數～數十）；共用每日 200 硬限額；查無/直幅/失敗回 null（前端維持深色 fallback，寧素勿錯）。
- 查詢詞：`day.cityEn`（生成時 LLM 順帶輸出，零額外成本）→ cityEn 對照表 → 空白行程「Taiwan aerial landscape」。
- ❗上架前重評：Pexels 授權允許商用；規模化可申請提額（免費）。
- **待辦**：Kelvin 需到 pexels.com/api 註冊取金鑰 → `supabase secrets set PEXELS_KEY=...` → 重新部署 ai-proxy。

## 3.7 回憶照片（批⑤c · 旅程相簿）
- **儲存**：壓縮後 ~250–400KB/張；上限＝一律 150 張/趟（規則單純好溝通；天花板不變） → 極端 ~45MB/趟。Supabase 免費層 1GB storage——自用無虞；**規模化前要重評**（10 使用者 × 3 趟 × 45MB ≈ 1.35GB 即破免費層）。
- **Egress（真炸點）**：播放膠卷＝重載大圖，重複觀看吃 egress（先前已撞過 5GB 上限）。**✅ 縮圖層已做（2026-07-31 效能量測批）**：上傳雙檔（原圖＋~480px/0.72 影子縮圖 `uuid_t.jpg`，命名衍生不動 schema）；膠卷漸進式＝縮圖立即上畫、停留 400ms 才抓當前格大圖（連翻不下載大圖）；舊照無縮圖自動退回大圖。量測依據：iPhone 真機 profile，大圖首次上畫＝解碼 400~1100ms（膠卷頓的真兇）、翻頁引擎本身 60fps 無罪。**✅ 封面縮圖也已做（同日封面縮圖小批；test-05 量測翻回憶頁 390~580ms 慢幀＝封面 1200px 首次解碼）**：回憶卡 208px 改用小圖——自家封面上傳走影子縮圖管線（uploadTripImageWithThumb，兩處封面上傳入口都換）、Pexels 封面 URL 參數直接要 640×350（CDN 即時縮放，零 API）；舊封面無影子檔退回大圖（要受益需重新上傳封面或未來回填批）。
- **精選膠卷**（記錄待辦）：相簿（倉庫，最多 150）≠ 膠卷（精選 15–24 張，`memoryReelPaths` 子集）；選片 UI 獨立小批。精選同時也是 egress 護欄（預設只播精選）。

## 4. 用量／花費記錄（定期自己填；建議每月一次對帳）

| 期間 | Places 搜尋 | findplace/resolve | Geocode | Directions | Gemini | 估算花費(USD) | 實際帳單 | 備註 |
|---|---|---|---|---|---|---|---|---|
| （範例）2026-07 |  |  |  |  |  |  |  | 開發測試期 |
|  |  |  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |  |  |

> 用量哪裡看：Google Cloud Console → APIs & Services → Metrics（各 API 呼叫數）；帳單 → Billing。
> `geocode_usage` 表也可看每人每日 Google 呼叫數（搜尋＋geocode 合計）。

---

_最後更新：D2 剩項定案（貼連結＋評分方案A）記入成本護欄；評分只在存清單/開細節查一次 Details、納每日限額＋快取。_
