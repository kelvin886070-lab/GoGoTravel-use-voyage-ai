# voyage-ai 設計與驗收清單（2026-07）

> 用途：逐條測試驗收、並在每節末的「我的想法」寫下你要融合的調整。
> 產品脊椎：**存 → 排 → 補 → 走**（capture → arrange → book gaps → execute）。

---

## 0. 動工前的環境動作（不做這些，下面有些會失效）

- [ ] 跑 SQL（BuyHere 綁定用）：`alter table public.wish_items add column if not exists stop_id text;`
- [ ] 部署 Edge Function（R2a 座標偏置用）：`supabase functions deploy ai-proxy`
- [ ] 確認雲端環境變數 `GOOGLE_GEOCODING_KEY`、`VITE_GOOGLE_MAPS_KEY`、`VITE_GOOGLE_MAP_ID` 已設定

---

## A. 已上線・可立即驗收

### A1. 購物 WISH：「在這裡要買」per-stop 副卡

一句話：把脆弱的「自動配對」換成「購物型 stop 自動浮候選＋一鍵勾」，卡片份量對齊交通副卡、不搶主視覺。

- [ ] 進行程 → 購物型 stop（tag=購物）卡片下方出現 `＋ 這裡要買？· N 待買`
- [ ] 點入口 → picker 一鍵＋把品項綁到這站（>8 樣才浮標籤篩選）
- [ ] 綁了收合成一行 `在這裡要買 · 2 · 1/2 已買`；展開＝發票明細（可勾、可移除、可再加）
- [ ] 勾「已買」→ 同步回購物清單與代購結算
- [ ] 兩天各一間 Lawson → 各綁各的，不混淆
- [ ] 拖曳重排該 stop → 綁定不斷（activity id 已回填）
- [ ] 非購物 stop 不主動浮（時間軸保持清爽）

我的想法：

### A2. 本趟購物清單 ＋「附近」排序

- [ ] 靈感心願盒 → 購物清單 tab 顯示本趟購物（依店家分組）
- [ ] 「附近」鈕 → 授權定位 → 店家依距離排序＋距離膠囊

我的想法：

### A3. 貼上匯入：混合自動分類（省一頁）

- [ ] 新增收藏＝純自動入口（沒有前置的地點/購物選擇）
- [ ] 貼混合清單 → 辨識 → 預覽分「地點／購物」兩區
- [ ] 逐列 `⇄ 移到購物清單／地點`；分區標題 `整區移到…`
- [ ] 匯入鈕顯示 `匯入 N 地點 · M 購物`；分別進地圖與購物清單

我的想法：

### A4. 三層定位（免費 geocode 收斂）

- [ ] 「整批地區」欄位（國家＋城市，選填）→ 補地點列 country/city → 分桶正確＋查詢自動偏置
- [ ] 沒填地區又有地點 → 匯入前提醒一次（仍匯入／回去指定）
- [ ] 幾何離群偵測：明顯脫離群集的點標「位置待確認」，不靜默亂放
- [ ] （需部署後）consensus 座標偏置：可信點取重心，把弱點/離群點自動重查修回

我的想法：

### A5. 批次位置確認地圖

- [ ] 「位置待確認 · N」膠囊 → 開批次地圖
- [ ] 離群紅、待確認琥珀；可信點不打擾
- [ ] 每列「地圖上修」單點微調；「全部確認」一鍵清；「稍後」不擋

我的想法：

### A6. 文案去「AI」

- [ ] 貼上匯入提示／placeholder、行程「靈感推薦」、建立行程「跳過」、登入頁 footer 皆不再出現「AI」

我的想法：

### A7. 變臉首頁 —「今天/走」臉（旅程中）

一句話：今天落在某趟行程日期內時，首頁自動從「行程卡列表」翻成「今天/走」執行面。

- [ ] 把某趟行程日期改成包含今天 → 首頁自動變臉
- [ ] 下一站大焦點：站名、幾分後、距離（需開定位）、「導航」deep-link 到 Google Maps、「看詳情」進行程
- [ ] 今日行程：收合＝時間軸 chips（當前綠、過去劃線）；「看全日」展開整天
- [ ] 今天各站有綁定未買 → 浮「今天有 N 樣要買」
- [ ] 「所有行程」→ 完整行程卡列表；點底部「首頁」→ 回今天臉
- [ ] 今天走完＝「都走完了」；今天沒排＝「還沒排行程」
- 註：距離為直線 haversine（免費）；實際路程交給 Google Maps；即時 ETA 為日後可選加法。

我的想法：

---

## B. 已設計・待實作（給你先反應、融合想法）

### B1. 變臉首頁 — 規劃臉的「補齊」設計（D＋C 混合）

- 你的行程卡設計**完全不動**。
- 卡片底邊：**D 四段就緒條**（機票／住宿／活動／文件；綠=好、琥珀=缺、灰=空）→ 多行程一眼掃健康度。
- 點卡片本身＝照舊開行程（行為不變）。
- 點就緒條＝原地展開 **C 就緒膠囊列**做補齊；缺的膠囊即 CTA。

我的想法：

### B2. 就緒偵測邏輯 ＋ 補齊決策點

- 本質：偵測的是「App 已知的就緒」，不是「真實就緒」。
- 訊號：機票＝flight 卡/電子機票；住宿＝hotel 卡蓋滿每晚；活動＝每天有無排；文件＝保管箱。
- 四態：已備(綠)／缺(琥珀·可訂)／不適用(灰)／已自理(使用者標記)。
- 關鍵：缺口必須能**一鍵和解**，避免「狼來了」。兩個出口＝`匯入實際文件`（順便進保管箱）或 `標記已自理`。和解動作本身＝訂購漏斗（還沒訂→幫你訂／已訂→標記）。
- 先做機票、住宿（有/無最明確、變現價值最高）；活動、文件次要。

我的想法：

### B3. 保管箱 UX 重新設計（B2 的前置依賴）

- 新角色：文件匯入與就緒「和解落腳處」，不再只是檔案櫃＋垃圾桶。
- 待深入討論。

我的想法：

### B4. 分頁（tab）預算收斂

- 探索：退場（被動、低頻、偏外部）。
- 會員管理：先放首頁右上頭像，交易上線再升 tab。
- 小工具：你之後會拿掉 → 不再是分頁。
- 保管箱：待議。
- 趨勢：可能收到很乾淨的 3 格（首頁／靈感／保管箱），首頁變臉、走不佔 tab。

我的想法：

---

## C. 商業模式與策略決策（本輪對齊）

- **楔子**：行程缺口偵測＋主動提醒（純軟體、吃現有資料、高價值）。
- **變現路徑（精實、可驗證）**：提醒 → 聯盟導流抽佣（零庫存/牌照/客服）→ 驗證有需求 → 才考慮聚合 API 站內訂 → 「直接當旅行社」是後期重資本，不是創業假設。
- **紅隊提醒**：庫存與合約才是護城河（非 app 完整度）；收錢就擁有所有失敗模式（客服/退改/法規牌照）；Dcard 通路「流量≠成交意圖」，你對它的不可取代價值要先想清楚。
- **geofence 在地推播**：等原生殼（Capacitor＋背景定位＋APNs/FCM）。邏輯與資料面現已就緒，屆時是「接上」非「重寫」。

我的想法：

---

## D. open threads（等你開場的下一階段）

1. 保管箱 UX 重設計（→ 解鎖就緒偵測）。
2. 就緒偵測門檻（活動「排幾個算夠」、文件是否必需）。
3. 會員頁升 tab 的時機（綁定交易/付費上線）。
4. 靈感頁（存）是否要微調。

我的想法：
